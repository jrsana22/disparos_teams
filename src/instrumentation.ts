// Cron is handled by Vercel Cron Jobs (vercel.json) in production.
// For local development, use: curl -X POST http://localhost:3000/api/cron -H "x-cron-secret: <CRON_SECRET>"
export async function register() {}
